# Handoff — 2026-02-16

## Project Status

The bracket-pool-dapp MVP is **feature-complete** and has been **end-to-end tested on local Anvil**. All three layers (contracts, scorer, frontend) are integrated and working.

| Layer | Status | Tests |
|-------|--------|-------|
| Smart Contracts (Foundry) | Complete | 64 Solidity tests pass |
| Off-Chain Scorer (TypeScript) | Complete | 23 vitest tests pass |
| Frontend (Next.js + wagmi) | Complete | Builds cleanly, manually tested |

### What Was Verified in E2E Testing (2026-02-16)

1. Deployed MockUSDC, Factory, and test Pool to local Anvil
2. Minted USDC to test accounts, submitted entries from 3 accounts (2 via cast, 1 via browser)
3. Verified bonding curve pricing (10 USDC base, 1% slope)
4. Warped time, posted results, ran scorer
5. Verified 5% fee transfer to treasury (integer division matches between scorer and contract)
6. Posted Merkle root on-chain
7. Claimed prize via browser UI — pool drained correctly, winner received full prize
8. All financial accounting verified: sum(pricePaid) == totalPoolValue, fee + prizePool == totalPoolValue

### Wallet Connection Fix

RainbowKit requires a valid WalletConnect project ID. For local development without one, the app now falls back to injected-only (MetaMask) mode:

- `web/src/lib/wagmi.ts` — dual config: RainbowKit when `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` is set and not `PLACEHOLDER`, injected-only otherwise
- `web/src/components/WalletButton.tsx` — conditionally renders RainbowKit's ConnectButton or a simple injected wallet button
- `web/src/app/providers.tsx` — conditionally wraps with RainbowKitProvider

---

## Local Development Setup

Anvil state resets on restart. To get back to a working state:

```bash
# Terminal 1 — start Anvil
cd ~/madness-app/bracket-pool-dapp/contracts
~/.foundry/bin/anvil

# Terminal 2 — deploy contracts
cd ~/madness-app/bracket-pool-dapp/contracts
export PATH="$HOME/.foundry/bin:$PATH"

# Deploy MockUSDC
forge create test/mocks/MockUSDC.sol:MockUSDC \
  --rpc-url http://127.0.0.1:8545 \
  --private-key 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 \
  --broadcast

# Update contracts/.env with MockUSDC address, then deploy factory
forge script script/Deploy.s.sol --rpc-url http://127.0.0.1:8545 --broadcast

# Terminal 3 — start frontend
cd ~/madness-app/bracket-pool-dapp/web
npm run dev
# Open http://localhost:3000
```

### MetaMask Configuration

- Network: Anvil Local, RPC: `http://127.0.0.1:8545`, Chain ID: `31337`
- Import Anvil account #0 private key: `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`
- Use Chrome/Brave (Safari doesn't support wallet extensions)

### Environment Files (not tracked by git)

**`contracts/.env`:**
```
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
USDC_ADDRESS=<deployed MockUSDC address>
TREASURY_ADDRESS=0x70997970C51812dc3A010C7d01b50e0d17dc79C8
```

**`web/.env.local`:**
```
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=PLACEHOLDER
NEXT_PUBLIC_FACTORY_ADDRESS=<deployed factory address>
```

---

## Remaining Work

### ~~1. Test Refund Flow in Browser~~ — DONE (2026-02-16)
All three refund scenarios verified end-to-end in the browser on local Anvil:
- **Pool cancelled by admin** — Refund panel appeared with "Pool cancelled", refund tx succeeded
- **Insufficient entries after lock time** — Refund panel appeared with "Not enough entries after lock time", refund tx succeeded
- **Past finalize deadline, no merkle root** — Refund panel appeared with "Finalization deadline passed without results", refund tx succeeded

**Bug found & fixed:** `DeployLocal.s.sol` was creating pools with `gameCount=67` but the frontend bracket structure only supports 63 games (no First Four). Changed to `gameCount=63`.

### 2. UX Improvements
- Randomize button on bracket picker is easy to miss (subtle gray text) — make more prominent
- General polish pass on the UI

### 3. Get WalletConnect Project ID
- Register at https://cloud.walletconnect.com
- Set `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` in `.env.local` to enable RainbowKit (supports WalletConnect, Coinbase Wallet, etc.)

### 4. Testnet Deployment (Sepolia)
- Deploy factory to Sepolia
- Use real Sepolia USDC or deploy a test token
- Deploy frontend to Vercel
- Full E2E test on testnet

### 5. Production Readiness
- Security audit / peer review
- Deploy to mainnet
- Set up Gnosis Safe multisig for admin/treasury
- Verify contracts on Etherscan

---

## Key Architecture Decisions

Documented in `docs/phase-0-decisions.md`. Highlights:
- **63 games** (no First Four — full NCAA bracket without play-in games), 6 rounds with ESPN-style scoring
- **Hash-only storage** — contract stores `keccak256(picks)`, full picks emitted in events
- **5% fee** via `totalPoolValue * 500 / 10000` (Solidity integer division)
- **Merkle tree claims** — scorer generates tree, root posted on-chain, proofs hosted on IPFS
- **Claim deadline** — `finalizeDeadline + 90 days`, then admin can sweep unclaimed funds
- **Tiebreaker** — predicted championship total score, closest wins, ties split evenly

## Reference Docs

- `docs/phase-0-decisions.md` — design decisions and rationale
- `docs/whitepaper.md` — project overview for team recruitment
- `docs/future-ideas.md` — post-MVP feature ideas
- `docs/plans/2025-02-09-bracket-pool-mvp-updated.md` — high-level design plan
- `docs/plans/2025-02-10-bracket-pool-implementation.md` — detailed implementation plan
- `docs/plans/2025-02-10-world-cup-pivot-design.md` — World Cup expansion design
